#!/bin/sh

dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

files="bashrc vimrc tmux.conf irbrc"

for file in $files; do
	if [[ -f ~/.$file ]]; then
		mkdir -p ~/dotfiles-old
		mv ~/.$file ~/dotfiles-old/$file
	fi
	ln -s $dir/$file ~/.$file
done
